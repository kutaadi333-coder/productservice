server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${TEST_DB_NAME:user_db}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:Shiva123@}
  jpa:
    hibernate:
      ddl-auto: create-drop

logging:
  level:
    root: WARN